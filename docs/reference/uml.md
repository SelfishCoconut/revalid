# UML diagrams (generated)

Regenerated from the actual code by `pyreverse` on every docs build (`make docs`, CI). Never edit by hand — they cannot be stale.

## Class diagram

```mermaid
--8<-- "docs/reference/generated/classes_revalid.mmd"
```

## Package diagram

```mermaid
--8<-- "docs/reference/generated/packages_revalid.mmd"
```
